extends layout

block content
	ul.archive
	-var lastYear = 0
	each post,index in posts
		if(lastYear !=post.time.year)
			li #{post.time.year}
		if(lastYear =post.time.year)
			li #{post.time.day}
			li
				a(href='/user/#{post.name}/#{post.time.day}/#{post.title}') #{post.title}